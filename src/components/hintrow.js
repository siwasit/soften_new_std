import React from 'react'

const Hintrow = () => {
  return (<></>
    // วิธี Loop
    // {data.length === 0 ? (
    //     <div className="text-center h1 mt-5 pt-5">Not found</div>
    // ) : (
    //     <div className="showroom row overflow-auto">
    //         {data.map((product) => (
    //             <div className="col-lg-4 col-md-6 col-sm-12 mb-3" key={product.id}>
    //                 <Card data = {product} />
    //             </div>
    //         ))}
    //     </div>
    // )}
  )
}

export default Hintrow